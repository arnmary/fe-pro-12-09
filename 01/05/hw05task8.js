//Створіть функцію, яка приймає число і повертає масив об'єктів. Кожен об'єкт має містити два поля: число і його квадрат. Масив повинен містити об'єкти для чисел від 1 до цьго чила . Наприклад
// [
//   { number: 1, square: 1 },
//   { number: 2, square: 4 },
//   { number: 3, square: 9 },
//   { number: 4, square: 16 },
//   { number: 5, square: 25 }
// ]
let nevArr =[];
console.log(nevArr);


function squareNumbs(a) {
 return a**2;
 
}  
console.log(`test${squareNumbs(1)}`);
squareNumbs();

let firstObject ={};
function addItems(object, a) {
  let firstItem =object.number =a;
  let secondItem =object. square = a**2;
object = firstObject;
 return firstItem, secondItem;

 
}

addItems( firstObject,(1))
console.log(addItems);

console.log(firstObject);

nevArr.push(firstObject);
console.log(nevArr);

let secondObject ={};

addItems(secondObject,(2));
nevArr.push(secondObject);

let thirdObject ={};

addItems(thirdObject,(3));
nevArr.push(thirdObject);

let forthObject ={};
addItems(forthObject,(4));
nevArr.push(forthObject);

let fifthObject ={};
addItems(fifthObject,(5));
nevArr.push(fifthObject);

